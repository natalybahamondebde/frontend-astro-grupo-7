---
import {Image} from "astro:assets";
const { image } = Astro.props;
---

<div class="bg-gray-500 aspect-video">
    <Image src={image} inferSize={true} alt="img" class="h-full w-full object-cover" />
</div>